#include "Renderable.hpp"
#include <typeinfo>
/*

template<CanRender T>
inline Renderable<T>::Renderable(RenderWindow& window, std::string path, int32_t x, int32_t y, uint8_t sections)
{
	instance.load(window, path, x, y, sections);
}

template<CanRender T>
inline void Renderable<T>::load(RenderWindow& window, std::string path, int32_t x, int32_t y, uint8_t sections) {
	instance.load(window, path, x, y, sections);
}

template<CanRender T>
void Renderable<T>::render(RenderWindow& window, SDL_Rect dst) {


}

*/

